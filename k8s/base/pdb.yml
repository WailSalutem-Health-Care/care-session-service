apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: care-session-service-pdb
  namespace: wailsalutem-suite
spec:
  minAvailable: 2  # Always keep at least 2 pods running
  selector:
    matchLabels:
      app: care-session-service
